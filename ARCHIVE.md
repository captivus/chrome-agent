# Archive

The public tree is the chrome-agent run-kit plus current tests.

`planning/`, `docs/`, `AGENTS.md`, `TODO.md`, and the cdp-wait proof scripts
moved to `archive/` (gitignored). Factory dirs (`adws/`, `specs/`, `requests/`,
`app_docs/`) are gitignored so a future SSSF stamp stays local.

Do not restore extras to GitHub.

This public copy tracks captivus/chrome-agent; do not commit private AGENTS
material.
